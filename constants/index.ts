import Colors from './colors';
import Enums from './enums';
import Fonts from './fonts';
import Metrics from './metrics';
import Strings from './strings';
import Theme from './theme';
const shadow = {
  shadowColor: '#000000',
  shadowOpacity: 0.175,
  shadowRadius: 10,
  shadowOffset: {
    height: 0,
    width: 0,
  },
  elevation: 10,
};
export {Colors, Enums, Fonts, Metrics, Strings, Theme, shadow};
